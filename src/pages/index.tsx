import Image from 'next/image'
import type { Metadata } from 'next'

export default function Page() {
  return (
    <main className="bg-white h-full w-full">
      <a href="https://assets.musicle.app/img/logo.svg" download="logo">Teeste</a>
    </main>
  )
}
